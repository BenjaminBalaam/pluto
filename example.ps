string a = "abc";

/*
void my_function(list<string> input = 0, float **test)
{
    Function<string, float> test = (string name) -> float {
        if (my_function(0, abc + 10 / 2))
        {
            float t = a % b;
        }

        for (int i = 0; i < length; i += 1)
        {
            print(i);

            if (isprime(i))
            {
                break;
            }
        }
    };
}
*/