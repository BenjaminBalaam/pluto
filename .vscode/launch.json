{
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "/usr/bin/make",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb"
        }
    ]
}
